# Reconcile fixture provenance

Canned read-API responses for reconcile diff tests. All `synthetic: true`,
built from the official API shapes: Seerr `/api/v1/request`, Radarr/Sonarr
`/api/v3/history`, Tautulli `/api/v2?cmd=get_history`. Replace with real
captures once shadow ingest is running (rollout step 2, "reconcile
confidence").
